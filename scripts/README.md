# 🎯 Bash Scripts для macOS

Автоматизированные скрипты для запуска всех этапов проекта Credit Scoring MLOps.

## 📋 Список скриптов

### 🚀 Главный скрипт установки

**`setup.sh`** - Первоначальная настройка проекта
```bash
./setup.sh
```
- Проверяет все зависимости
- Создает виртуальное окружение
- Устанавливает Python пакеты
- Создает структуру директорий

---

### 🔄 Скрипты ML Pipeline

#### **`scripts/train_model.sh`** - Обучение модели
```bash
./scripts/train_model.sh
```
- Обучает нейронную сеть
- Сохраняет модель и scaler
- Создает логи обучения

#### **`scripts/convert_onnx.sh`** - Конвертация в ONNX
```bash
./scripts/convert_onnx.sh
```
- Конвертирует PyTorch → ONNX
- Валидирует конвертацию
- Проводит benchmark тесты

#### **`scripts/optimize_model.sh`** - Оптимизация модели
```bash
./scripts/optimize_model.sh
```
- Применяет квантизацию (INT8)
- Сравнивает производительность
- Генерирует отчет оптимизации

#### **`scripts/check_drift.sh`** - Мониторинг дрифта
```bash
./scripts/check_drift.sh
```
- Анализирует дрифт данных
- Генерирует HTML отчеты
- Открывает отчеты в браузере

---

### 🌐 Скрипты API

#### **`scripts/run_api.sh`** - Запуск API сервера
```bash
./scripts/run_api.sh
```
- Запускает FastAPI приложение
- Доступен на http://localhost:8000
- Автоматически настраивает переменные окружения

#### **`scripts/test_api.sh`** - Тестирование API
```bash
./scripts/test_api.sh
```
- Тестирует все endpoints
- Проверяет валидацию
- Тестирует предсказания

---

### ⚡ Полный пайплайн

#### **`scripts/run_all.sh`** - Запуск всего пайплайна
```bash
./scripts/run_all.sh
```
Выполняет все этапы последовательно:
1. Обучение модели
2. Конвертация в ONNX
3. Оптимизация
4. Проверка дрифта
5. Запуск и тестирование API

---

### 🧹 Утилиты

#### **`scripts/clean.sh`** - Очистка проекта
```bash
./scripts/clean.sh
```
- Удаляет модели
- Очищает логи
- Удаляет отчеты
- Чистит Python cache

---

## 🎮 Быстрый старт

### Вариант 1: Пошаговый
```bash
# 1. Установка
./setup.sh

# 2. Обучение
./scripts/train_model.sh

# 3. ONNX
./scripts/convert_onnx.sh

# 4. Оптимизация
./scripts/optimize_model.sh

# 5. API
./scripts/run_api.sh

# В другом терминале: тестирование
./scripts/test_api.sh
```

### Вариант 2: Автоматический
```bash
# Установка
./setup.sh

# Запуск всего пайплайна
./scripts/run_all.sh
```

---

## 📝 Примеры использования

### Тренировка и запуск API
```bash
./setup.sh
./scripts/train_model.sh
./scripts/convert_onnx.sh
./scripts/optimize_model.sh
./scripts/run_api.sh &  # В фоне

# Тестируем
./scripts/test_api.sh

# Останавливаем API
pkill -f "uvicorn"
```

### Только проверка дрифта
```bash
./scripts/check_drift.sh
```

### Полная очистка и пересборка
```bash
./scripts/clean.sh
./scripts/run_all.sh
```

---

## 🔧 Требования

Скрипты проверяют и требуют:
- macOS (или Linux с bash)
- Python 3.9+
- Homebrew (для установки зависимостей)
- Git
- Docker (опционально)

---

## 💡 Полезные команды

### Просмотр логов
```bash
# Логи обучения
tail -f logs/training_*.log

# Логи API
tail -f logs/api_*.log

# Логи дрифта
tail -f logs/drift_monitoring_*.log
```

### Проверка процессов
```bash
# Проверить запущен ли API
lsof -i :8000

# Остановить API
pkill -f "uvicorn"
```

### Активация окружения вручную
```bash
source venv/bin/activate
```

---

## 🐛 Решение проблем

### Ошибка: "Permission denied"
```bash
chmod +x setup.sh
chmod +x scripts/*.sh
```

### Ошибка: "command not found"
```bash
# Установите Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите Python
brew install python@3.9
```

### API не запускается
```bash
# Проверьте наличие модели
ls -lh models/optimization/credit_scoring_quantized.onnx

# Проверьте порт
lsof -i :8000
```

### Модель не обучается
```bash
# Проверьте логи
cat logs/training_*.log

# Проверьте зависимости
pip list | grep torch
```

---

## 📚 Дополнительная информация

- **Документация проекта:** `cat README.md`
- **Быстрый старт:** `cat QUICKSTART.md`
- **Подробная инструкция:** `cat SETUP.md`
- **Архитектура:** `cat ARCHITECTURE.md`

---

## 🎓 Порядок выполнения для сдачи проекта

1. **Установка:** `./setup.sh`
2. **Полный пайплайн:** `./scripts/run_all.sh`
3. **Скриншоты:**
   - Swagger UI: http://localhost:8000/docs
   - Отчеты дрифта: `open monitoring/evidently/reports/*.html`
   - Результаты тестов в терминале
4. **Документация:** обновите README с вашими результатами
5. **Git:** загрузите в репозиторий

---

## ⚠️ Важные заметки

- Скрипты автоматически активируют виртуальное окружение
- Все логи сохраняются в `logs/` с timestamp
- API запускается на порту 8000
- Модели сохраняются в соответствующих директориях
- Скрипты выходят при первой ошибке (`set -e`)

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в `logs/`
2. Убедитесь что все зависимости установлены
3. Проверьте версию Python: `python3 --version`
4. Создайте Issue в GitHub репозитории

---

**Удачи с проектом! 🚀**
